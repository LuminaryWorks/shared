{
  "name": "@luminaryworks/ai-client",
  "version": "0.1.1",
  "license": "Polyform-Noncommercial-1.0.0",
  "description": "LuminaryWorks AI Platform client — complete, stream, embed, local BYOK adapter",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist/**/*.js", "dist/**/*.d.ts", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/*.spec.js || true"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
